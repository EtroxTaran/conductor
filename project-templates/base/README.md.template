# {{PROJECT_NAME}}

<!-- Project created: {{CREATION_DATE}} -->
<!-- Template: base v1.0.0 -->

## Overview

[Brief description of this project]

## Structure

```
{{PROJECT_NAME}}/
├── CLAUDE.md           # Worker Claude context (auto-generated)
├── GEMINI.md           # Gemini reviewer context (auto-generated)
├── .cursor/rules       # Cursor reviewer context (auto-generated)
├── PRODUCT.md          # Feature specification (edit this)
├── .workflow/          # Workflow state and phases
├── project-overrides/  # Project-specific rule overrides
├── src/                # Source code
└── tests/              # Test files
```

## Getting Started

1. Edit `PRODUCT.md` with your feature specification
2. From the meta-architect root, run: `/orchestrate --project {{PROJECT_NAME}}`

## Project-Specific Rules

To add rules that apply only to this project:

1. Create files in `project-overrides/`:
   - `claude.md` - Extra rules for worker Claude
   - `gemini.md` - Extra rules for Gemini reviewer
   - `cursor.md` - Extra rules for Cursor reviewer

2. Run `/sync-projects --project {{PROJECT_NAME}}` to merge with templates

## Workflow State

The `.workflow/` directory contains:
- `state.json` - Current workflow state
- `phases/` - Output from each phase

## Commands

From meta-architect root:

```bash
# Start workflow for this project
/orchestrate --project {{PROJECT_NAME}}

# Check project status
/phase-status --project {{PROJECT_NAME}}

# Sync template updates to this project
/sync-projects --project {{PROJECT_NAME}}
```
