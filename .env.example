# SurrealDB Configuration
# ========================
# Copy this file to .env and update with your values.
# You can also place a global config at ~/.config/meta-architect/.env

# SurrealDB Connection URL (required to enable SurrealDB)
# Leave empty or unset to use file-based storage (default)
# Format: ws://hostname:port/rpc or wss://hostname:port/rpc
# SURREAL_URL=ws://localhost:8000/rpc
SURREAL_URL=

# Namespace for isolation (all project databases are created within this namespace)
SURREAL_NAMESPACE=orchestrator

# Authentication credentials
SURREAL_USER=root
SURREAL_PASS=

# Default database (used when no project is specified)
# Each project gets its own database: my-project -> my_project database
SURREAL_DATABASE=default

# Connection Pool Configuration
# Number of connections to maintain in the pool
SURREAL_POOL_SIZE=5

# Timeouts (in seconds)
SURREAL_CONNECT_TIMEOUT=10.0
SURREAL_QUERY_TIMEOUT=30.0

# Retry Configuration
SURREAL_RETRY_ATTEMPTS=3
SURREAL_RETRY_DELAY=1.0

# Enable Live Query subscriptions for real-time monitoring
SURREAL_LIVE_QUERIES=true

# Skip SSL verification (NOT recommended for production)
# Only set to true for self-signed certificates in development
SURREAL_SKIP_SSL_VERIFY=false
